{% set data = {
  title: ''
}|merge(data ?? {}) %}

<section class="ud-page-banner">
  <div class="container">
    <div class="row">
      <div class="col-lg-12">
        <div class="ud-banner-content">
          <h1>{{ data.title }}</h1>
        </div>
      </div>
    </div>
  </div>
</section>